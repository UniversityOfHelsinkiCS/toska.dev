apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: toska-dev
  annotations:
    custom-hostname: staging.toska # staging.toska.dev
spec:
  template:
    spec:
      containers:
        - image: toska/toska-dev